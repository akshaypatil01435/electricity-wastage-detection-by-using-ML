# WattVision AI Frontend Configuration
# Spring Boot REST API base URL
VITE_API_BASE_URL=http://localhost:8080/api

# true  = mock data + demo login (presentations, works without the backend)
# false = real Spring Boot API with real JWT login (start the backend first)
VITE_DEMO_MODE=true
